import 'package:flutter/cupertino.dart';

class TopicWidgdet extends StatelessWidget {
  const TopicWidgdet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
